export class App{
    static listaErrores(json){
        let res = "<strong>Errores: </strong> <br><br>";
        var errores = JSON.
        console.log(errores)
        // json.each((i, item) => {
        //     res += "-" + item.toString() + "<br>";
        // })
        return res;
    }
}